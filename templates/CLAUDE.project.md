# CLAUDE.md

<!--
Spend most of this file's tokens on GOTCHAS. That is the part no one can infer.

Stack, layout, and entry points are visible from the file tree, the manifest, and
the imports - stating them costs context every session to tell Claude something it
would have learned in one `ls`. State the things that would take an hour to discover
and look like bugs until you know: the field that must stay UTC, the generated
directory that must never be hand-edited, the test that only passes with the dev
server up, the migration that has to run before the seed.

Do NOT restate global behavior. It already applies:
- Behavioral floor (planning, scope, verification, commits, error handling):
  ~/.claude/SOUL.md
- Routing residuals and fan-out economics: ~/.claude/CLAUDE.md
Repeating either is dead weight that costs context every session.

Do NOT write a routing table here either. Claude Code injects every skill's and
command's `description` each session - that listing IS the routing. If a skill or
command isn't firing when it should, fix its `description:` frontmatter rather than
adding an "X -> use Y" line here.
-->

<!-- Strategic Shift block - OPTIONAL. Use when a major repositioning, competitive
event, or pivot happens. Replace YYYY-MM-DD; cover what changed, new positioning,
moat thesis, phasing, and links to plan/audit files. Delete when stale. Keep it to a few lines. -->
<!--
## YYYY-MM-DD strategic shift - read first
[What changed] · [New positioning / moat thesis] · [Phasing] · [Backbone files]
-->

## What this is

[One or two sentences: what the project does and who it is for. Not a feature list.]

## Key Commands
<!-- Only the ones that are NOT inferable from the manifest. If `npm test` is just
`npm test`, delete the line. Keep the ones with a prefix, a flag, a required env
var, or an ordering constraint. -->
- **Build**: `[command]`
- **Test**: `[command]`
- **Lint / Type Check**: `[command]`
- **Dev Server**: `[command]`

## Architecture
<!-- Only the parts the file tree does not already show: what talks to what, where
the data actually flows, which boundary is load-bearing. Skip the directory listing. -->
- [1-2 sentences]

## Gotchas
<!-- THE MOST VALUABLE SECTION. Non-obvious behavior, things that look wrong but are
intentional, environment quirks, ordering constraints, footguns you already hit once.
Every entry here should be something that cost someone real time. -->
- [e.g. "the .xcodeproj is generated - run xcodegen before xcodebuild or you build yesterday's project"]
- [e.g. "all timestamps stored UTC, converted at the edge; a local-time value in the db is a bug"]
- [e.g. "never hand-edit /migrations - it's generated and the diff will be silently reverted"]

## Project Rules (sharpen SOUL, don't repeat it)
<!-- ONLY rules specific to this repo that genuinely conflict with or tighten the
global floor. Delete this section if there is nothing. Most repos have nothing. -->

## Docs Conventions
<!-- Tells /save which surfaces exist. Delete lines that don't apply. -->
- `docs/changelog.md` - what shipped, one bullet per change, newest first
- `docs/journal.md` - decisions + reasoning, newest first
- `CLAUDE.md` - this file. Update "Recent Changes" per session; `/clean` Phase 0 owns the size check.

## Worktree + Branch Model
<!-- Tells /ship whether to default to push or commit-only. Pick one, delete the other. -->
- **push to main from worktrees, no PR** - `/ship` with no args defaults to push
- OR **feature branches + PRs to main** - `/ship` defaults to commit-only; `/ship pr` opens a PR
- `auto-ship: ok` - OPTIONAL. Add this line to let Claude auto-fire `/ship` in this repo. Without it, shipping always waits for an explicit ask (global default).

## Recent Changes
<!-- Updated by /save after each session -->

## Known Issues / TODO

## References
- `~/.claude/SOUL.md` - global behavioral floor. Project rules add to it, never override.
- `~/.claude/CLAUDE.md` - routing residuals, fan-out economics.
- The project's auto-memory (`~/.claude/projects/<slug>/memory/`) - facts, loaded automatically.

<!-- Codex reads this file directly via project_doc_fallback_filenames; an AGENTS.md
symlink is not required. Cursor loads the same global layer through
~/.cursor/rules/*.mdc, regenerated by the kit's sync-cursor.sh. -->
